A summary with a concrete construct.

Concrete constructs cannot be interrupted by more containers.

```markdown
@internal
^
```

…`@internal` cannot form because micromark's fenced code construct is concrete.
